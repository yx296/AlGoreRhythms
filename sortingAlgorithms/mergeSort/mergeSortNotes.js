// merge(mergeSort([34, 203]), mergeSort([3, 15, 2]));

// mergeSort([34, 203])
// merge([34], [203])
// return [34, 203]

// mergeSort([3, 15, 2])
// var left = [3]
// var right = [15, 2]
// merge(mergeSort[3], mergeSort([15, 2])
// merge([3], [2, 15] )

// return [2, 3, 15]



// merge([34, 203], [2, 3, 15])
